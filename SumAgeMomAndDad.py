"جمع کردن" به انگلیسی "addition" یا "summing" می‌شود.

If you want to rename the function to reflect this, you can do so like this:

```python


def addition(Mom, Dad, Merge):
    Mom = int(input("Enter your Mom's age: "))
    Dad = int(input("Enter your Dad's age: "))
    Merge = Mom + Dad
    return Mom, Dad, Merge

print(addition(0, 0, 0))
```
